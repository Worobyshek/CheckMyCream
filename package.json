{
  "name": "check-my-cream",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:android:web": "node scripts/build-capacitor.mjs",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "cap:sync": "npx cap sync android",
    "cap:copy": "npx cap copy android",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android",
    "android:sync": "node scripts/build-capacitor.mjs && npx cap sync android",
    "android:run": "node scripts/build-capacitor.mjs && npx cap run android",
    "android:build": "node scripts/build-capacitor.mjs && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/cli": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "tesseract.js": "^7.0.0",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "eslint": "latest",
    "eslint-config-next": "latest",
    "prettier": "^3.3.1",
    "typescript": "latest"
  }
}
